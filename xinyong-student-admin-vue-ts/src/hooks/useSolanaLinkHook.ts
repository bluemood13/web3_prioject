import { tt } from "@/utils/tool";
export const useSolanaLinkHook = () => {
    // 定义远程变量
    // 获取远程下拉框数据
    const getRemoteSelData = (colLowName:string, key: string) => {
    }
    // 获取静态下拉框数据
    const getStaticSelData = (colLowName:string, key: string) => {
    }
    

  return {
    getRemoteSelData,
    getStaticSelData,
  };
};
